image:
  repository: sonarqube
  tag: 24.12.0.100206-community


flavors:
  - name: upstream
    # renovate-uds: datasource=docker depName=sonarqube
    version: 24.12.0.100206-uds.0

# junk drawer below
# original renovate comment:
# renovate-uds: datasource=docker depName=sonarqube extractVersion=^(?<version>\d+\.\d+\.\d+)\.\d+(-community)?$
